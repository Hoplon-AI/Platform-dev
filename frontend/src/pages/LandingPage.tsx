export function LandingPage() {
  return (
    <main style={{ padding: 24, fontFamily: "system-ui, sans-serif" }}>
      <h1 style={{ margin: 0 }}>Platform-dev</h1>
      <p style={{ marginTop: 8, maxWidth: 720 }}>
        Frontend scaffold (Vite + React + TypeScript). Next: wire upload flow to
        the backend API and add dashboard routes.
      </p>
    </main>
  );
}

